<!DOCTYPE html>
<html>
  <head>
    <%# 以下のタイトルタグに変数を埋め込む %>
    <title><%= yield(:title) %> | Railsチュートリアル SampleApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
